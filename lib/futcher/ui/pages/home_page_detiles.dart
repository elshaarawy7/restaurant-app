import 'package:flutter/material.dart';

class HomePageDetiles extends StatelessWidget {
  const HomePageDetiles({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold( 
      backgroundColor: Colors.amber,
    );
  }
}